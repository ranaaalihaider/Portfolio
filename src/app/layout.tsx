import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/ThemeProvider";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Ali Haider | Software Engineer",
  description: "Portfolio of Ali Haider, a Software Engineer building modern web, mobile, and full-stack applications.",
  openGraph: {
    title: "Ali Haider | Software Engineer",
    description: "Portfolio of Ali Haider, a Software Engineer building modern web, mobile, and full-stack applications.",
    url: "https://alihaider.dev",
    siteName: "Ali Haider Portfolio",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased bg-[var(--background)] text-[var(--foreground)] transition-colors duration-300`}>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
